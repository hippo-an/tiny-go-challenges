# Smoker: Kubernetes 클러스터 네트워크 진단 도구

## 1. 프로젝트 개요

### 1.1 목적
Smoker는 Kubernetes 클러스터 내부의 네트워크 상태를 지속적으로 모니터링하는 진단 도구입니다. Echo 서버와 진단 클라이언트로 구성되어 TCP, UDP, HTTP 네트워크 통신의 정상 작동 여부를 확인합니다.

### 1.2 핵심 가치
- 클러스터 네트워크 문제를 조기에 발견
- Node 간, Pod 간 통신 상태를 실시간으로 파악
- 간단한 배포와 운영으로 네트워크 가시성 확보

## 2. 현재 상태

### 2.1 구현 완료
- ✅ TCP Echo 서버 (`internal/server/tcp.go`)
  - 연결당 고루틴으로 동시 접속 처리
  - 30초 타임아웃 설정
  - 4096 바이트 버퍼 사용
- ✅ UDP Echo 서버 (`internal/server/udp.go`)
  - Connectionless 패킷 echo
  - 상태 비저장 구조
- ✅ HTTP Echo 서버 (`internal/server/http.go`)
  - `/` 엔드포인트: GET/POST 요청 echo
  - `/health` 엔드포인트: Health check
  - 30초 읽기/쓰기 타임아웃
  - 5초 graceful shutdown 타임아웃
- ✅ 서버 실행 진입점 (`cmd/server/main.go`)
  - 3개 서버 동시 실행 (고루틴)
  - 환경변수 기반 포트 설정
  - Graceful shutdown (SIGTERM/SIGINT)
  - WaitGroup과 에러 채널로 서버 관리
- ✅ TCP 클라이언트 (`internal/client/tcp.go`)
  - TCPPing 함수: "PING" 메시지 전송 및 응답 확인
  - Latency 측정 (milliseconds)
  - 타임아웃 처리
- ✅ 기본 프로젝트 구조
- ✅ CLAUDE.md 문서 (Claude Code용 가이드)

### 2.2 구현 필요
- ⬜ UDP 클라이언트 (`internal/client/udp.go`)
- ⬜ HTTP 클라이언트 (`internal/client/http.go`)
- ⬜ 클라이언트 실행 진입점 (`cmd/client/main.go`)
  - 30초 주기 실행
  - 환경변수 기반 설정
  - 구조화된 로그 출력
  - 노드 이름 감지
- ⬜ Kubernetes 매니페스트
  - Server Deployment + Service
  - Client DaemonSet
- ⬜ Dockerfile (Multi-stage build)
- ⬜ README.md (사용자 문서)
- ⬜ Makefile (빌드/배포 자동화)

## 3. 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                 Kubernetes Cluster                  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │          Server (Deployment)                 │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │  - TCP Echo  (Port 8080)              │  │  │
│  │  │  - UDP Echo  (Port 8081)              │  │  │
│  │  │  - HTTP Echo (Port 8082)              │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  │                                              │  │
│  │         Service (ClusterIP)                  │  │
│  │      smoker-server:8080/8081/8082           │  │
│  └──────────────────────────────────────────────┘  │
│                        ▲                           │
│                        │                           │
│         ┌──────────────┼──────────────┐            │
│         │              │              │            │
│  ┌──────┴─────┐ ┌─────┴──────┐ ┌────┴──────┐     │
│  │  Client    │ │  Client    │ │  Client   │     │
│  │ (DaemonSet)│ │ (DaemonSet)│ │(DaemonSet)│     │
│  │            │ │            │ │           │     │
│  │  Node 1    │ │  Node 2    │ │  Node 3   │     │
│  └────────────┘ └────────────┘ └───────────┘     │
│                                                     │
│  매 30초마다 TCP/UDP/HTTP 테스트 실행              │
└─────────────────────────────────────────────────────┘
```

## 4. 기능 요구사항 (MVP)

### 4.1 서버 컴포넌트

**배포 방식:** Deployment (Replicas: 1)

**기능:**
- **TCP Echo Server (Port 8080)**
  - 클라이언트의 TCP 연결을 수락
  - 받은 데이터를 그대로 반환 (Echo)
  - 동시 접속 처리 (Goroutine)
  - 연결 타임아웃: 30초

- **UDP Echo Server (Port 8081)**
  - 클라이언트의 UDP 패킷 수신
  - 받은 패킷을 송신자에게 그대로 반환
  - Connectionless 특성 반영

- **HTTP Echo Server (Port 8082)**
  - GET 요청: 200 OK + "OK" 메시지 반환
  - POST 요청: Body를 그대로 Echo
  - Health check 엔드포인트: `/health`

**로깅:**
- 요청 수신 로그
- 데이터 크기 및 클라이언트 주소 기록
- 에러 로그

### 4.2 클라이언트 컴포넌트

**배포 방식:** DaemonSet (모든 노드에서 실행)

**기능:**
- **주기적 테스트 실행**
  - 간격: 30초
  - 서버 주소: 환경변수로 설정

- **TCP Ping 테스트**
  - 서버에 "PING" 메시지 전송
  - 응답 "PING" 확인
  - 타임아웃: 5초

- **UDP Ping 테스트**
  - 서버에 "PING" 패킷 전송
  - 응답 패킷 확인
  - 타임아웃: 5초

- **HTTP Ping 테스트**
  - GET `/` 요청
  - 200 OK 응답 확인
  - 타임아웃: 5초

- **측정 항목**
  - 성공/실패 여부
  - 응답 시간 (Latency) - milliseconds
  - 에러 메시지 (실패 시)

**로그 형식:**
```
[2025-11-13 10:30:00] [NODE:node-1] [TCP] SUCCESS latency=15ms
[2025-11-13 10:30:00] [NODE:node-1] [UDP] SUCCESS latency=12ms
[2025-11-13 10:30:00] [NODE:node-1] [HTTP] SUCCESS latency=20ms
[2025-11-13 10:30:30] [NODE:node-2] [TCP] FAILED error=timeout
```

## 5. 기술 스택

### 5.1 개발
- **언어:** Go 1.25
- **의존성:** 표준 라이브러리만 사용
  - `net` - TCP/UDP 네트워킹
  - `net/http` - HTTP 서버/클라이언트
  - `log` - 로깅
  - `time` - 시간 측정
  - `os/signal` - Graceful shutdown
  - `context` - 컨텍스트 관리

### 5.2 배포
- **컨테이너:** Docker (Multi-stage build)
- **오케스트레이션:** Kubernetes
- **로그 수집:** kubectl logs (stdout)

### 5.3 제외 항목 (MVP 이후)
- Prometheus 메트릭 노출
- 설정 파일 지원 (YAML/JSON)
- 고급 통계 (P50, P95, P99)
- 알림 기능
- Web UI/Dashboard
- 멀티 클러스터 지원

## 6. 구현 계획

### Phase 1: 서버 완성 ✅ (완료)

**파일:**
- ✅ `internal/server/http.go` - HTTP Echo 서버 구현
- ✅ `cmd/server/main.go` - 서버 메인 진입점

**완료 사항:**
1. ✅ HTTP Echo 서버 구현
   - `/` - Echo 엔드포인트 (GET/POST)
   - `/health` - Health check
   - 30초 타임아웃 설정
2. ✅ 서버 메인 진입점 구현
   - 3개 서버를 goroutine으로 동시 실행
   - Graceful shutdown (SIGTERM/SIGINT 처리)
   - 환경변수로 포트 설정 (기본값: 8080, 8081, 8082)
   - `getEnvAsInt()` 헬퍼 함수로 안전한 환경변수 파싱

### Phase 2: 클라이언트 구현 (1시간) 🔄 (진행 중)

**파일:**
- ✅ `internal/client/tcp.go` - TCP 클라이언트 (일부 완료)
- ⬜ `internal/client/udp.go` - UDP 클라이언트
- ⬜ `internal/client/http.go` - HTTP 클라이언트
- ⬜ `cmd/client/main.go` - 클라이언트 메인 진입점

**완료 사항:**
1. ✅ TCP 클라이언트 일부 구현
   - `TCPPing(host, port, timeout)` 함수
   - "PING" 메시지 전송 및 응답 확인
   - Latency 측정 (milliseconds)
   - 에러 핸들링 및 응답 검증

**남은 작업:**
1. UDP 클라이언트 구현
   - `UDPPing(host, port, timeout)` 함수
   - UDP 패킷 전송 및 응답 확인
   - Latency 측정
   - 타임아웃 처리
2. HTTP 클라이언트 구현
   - `HTTPPing(host, port, timeout)` 함수
   - GET `/` 요청
   - 200 OK 응답 확인
   - Latency 측정
3. 클라이언트 메인 진입점 구현
   - 30초 주기 실행 (ticker)
   - 서버 주소 환경변수로 설정
   - 각 테스트 순차 실행 (TCP → UDP → HTTP)
   - 구조화된 로그 출력: `[TIMESTAMP] [NODE:node-name] [PROTOCOL] STATUS latency=XXms`
   - 노드 이름 환경변수에서 가져오기 (`NODE_NAME`)

### Phase 3: 컨테이너화 (30분)

**파일:**
- `Dockerfile` - Multi-stage build

**작업:**
1. Multi-stage Dockerfile 작성
   - Stage 1: Builder (Go 빌드)
   - Stage 2: Server 이미지
   - Stage 3: Client 이미지
2. 이미지 최적화 (Alpine base)
3. 로컬 빌드 테스트

### Phase 4: Kubernetes 배포 (30분)

**파일:**
- `deployments/server.yaml` - Server Deployment + Service
- `deployments/client.yaml` - Client DaemonSet

**작업:**
1. Server Deployment + Service 매니페스트
   - Replicas: 1
   - Service: ClusterIP
   - 3개 포트 노출 (8080, 8081, 8082)
2. Client DaemonSet 매니페스트
   - 모든 노드에 배포
   - 서버 Service DNS로 연결
   - 환경변수 설정

### Phase 5: 문서화 (30분)

**파일:**
- `README.md` - 프로젝트 문서
- `Makefile` - 빌드/배포 자동화

**작업:**
1. README.md 작성
   - 프로젝트 소개
   - 빌드 방법
   - 배포 방법
   - 로그 확인 방법
   - 트러블슈팅
2. Makefile 작성
   - `make build` - 로컬 빌드
   - `make docker-build` - Docker 이미지 빌드
   - `make deploy` - Kubernetes 배포
   - `make logs-server` - 서버 로그 확인
   - `make logs-client` - 클라이언트 로그 확인
   - `make clean` - 리소스 정리

## 7. 환경변수

### 7.1 서버
| 변수명 | 설명 | 기본값 |
|--------|------|--------|
| `TCP_PORT` | TCP 서버 포트 | 8080 |
| `UDP_PORT` | UDP 서버 포트 | 8081 |
| `HTTP_PORT` | HTTP 서버 포트 | 8082 |

### 7.2 클라이언트
| 변수명 | 설명 | 기본값 |
|--------|------|--------|
| `SERVER_HOST` | 서버 주소 | smoker-server |
| `TCP_PORT` | TCP 서버 포트 | 8080 |
| `UDP_PORT` | UDP 서버 포트 | 8081 |
| `HTTP_PORT` | HTTP 서버 포트 | 8082 |
| `TEST_INTERVAL` | 테스트 간격 (초) | 30 |
| `TEST_TIMEOUT` | 테스트 타임아웃 (초) | 5 |
| `NODE_NAME` | 현재 노드 이름 (로그용) | - |

**참고:** Kubernetes에서 `NODE_NAME`은 Downward API를 통해 자동 주입됩니다.

## 8. 테스트 시나리오

### 8.1 로컬 테스트
1. 서버 실행
   ```bash
   go run cmd/server/main.go
   ```

2. 클라이언트 실행 (별도 터미널)
   ```bash
   SERVER_HOST=localhost go run cmd/client/main.go
   ```

3. 로그 확인
   - 서버: 요청 수신 로그 확인
   - 클라이언트: 테스트 결과 및 latency 확인

### 8.2 Kubernetes 테스트
1. 배포
   ```bash
   kubectl apply -f deployments/
   ```

2. Pod 상태 확인
   ```bash
   kubectl get pods -l app=smoker
   ```

3. 로그 확인
   ```bash
   # 서버 로그
   kubectl logs -l app=smoker-server -f

   # 특정 노드의 클라이언트 로그
   kubectl logs -l app=smoker-client -f --max-log-requests=3
   ```

4. 네트워크 문제 시뮬레이션
   - 서버 Pod 재시작
   - 네트워크 정책으로 트래픽 차단
   - 클라이언트 로그에서 FAILED 확인

## 9. 성공 기준

### 9.1 기능
- ✅ 서버가 3개 포트에서 정상 실행
- ⬜ 클라이언트가 모든 노드에서 30초마다 테스트 실행
- ⬜ TCP/UDP/HTTP 모든 테스트 성공
- ⬜ 로그에서 각 테스트 결과와 latency 확인 가능
- ⬜ kubectl logs로 진단 정보 조회 가능

### 9.2 품질
- ✅ 에러 발생 시 적절한 로그 출력
- ✅ 타임아웃 처리 정상 작동
- ✅ Graceful shutdown 구현 (서버)
- ⬜ 메모리 누수 없음 (부하 테스트 필요)
- ✅ 동시 요청 처리 가능 (서버)

### 9.3 운영
- ⬜ Docker 이미지 정상 빌드
- ⬜ Kubernetes 배포 성공
- ⬜ DaemonSet이 모든 노드에 배포
- ⬜ Service로 서버 접근 가능
- 🔄 문서화 진행 중 (CLAUDE.md 완료, README.md 필요)

## 10. 향후 개선 사항

### 10.1 모니터링 강화
- Prometheus 메트릭 노출
- Grafana 대시보드
- 알림 규칙 설정

### 10.2 기능 추가
- DNS 해석 테스트
- TLS/SSL 지원
- gRPC 프로토콜 지원
- WebSocket 테스트

### 10.3 고급 진단
- 패킷 손실률 측정 (UDP)
- Jitter 측정
- 대역폭 테스트
- MTU 크기 탐지

### 10.4 운영 개선
- 설정 파일 지원
- 동적 설정 변경 (ConfigMap)
- 멀티 클러스터 지원
- 중앙 집중식 로그 수집

## 11. 예상 타임라인

| Phase | 작업 | 예상 시간 | 상태 |
|-------|------|-----------|------|
| Phase 1 | 서버 완성 | 30분 | ✅ 완료 |
| Phase 2 | 클라이언트 구현 | 1시간 | 🔄 진행 중 (30% 완료) |
| Phase 3 | 컨테이너화 | 30분 | ⬜ 대기 |
| Phase 4 | Kubernetes 배포 | 30분 | ⬜ 대기 |
| Phase 5 | 문서화 | 30분 | 🔄 진행 중 (30% 완료) |
| **총계** | | **3.5시간** | **약 30% 완료** |

**진행 상황:**
- ✅ 완료: 서버 구현 (TCP/UDP/HTTP), 서버 메인 진입점, CLAUDE.md
- 🔄 진행 중: TCP 클라이언트 일부 완료
- ⬜ 남은 작업: UDP/HTTP 클라이언트, 클라이언트 메인, Docker, K8s, README

## 12. 참고 자료

### 12.1 Go 네트워크 프로그래밍
- [net 패키지](https://pkg.go.dev/net)
- [net/http 패키지](https://pkg.go.dev/net/http)

### 12.2 Kubernetes
- [Deployment](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/)
- [DaemonSet](https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/)
- [Service](https://kubernetes.io/docs/concepts/services-networking/service/)

### 12.3 관련 도구
- [Linkerd](https://linkerd.io/) - Service mesh
- [Goldpinger](https://github.com/bloomberg/goldpinger) - 유사한 네트워크 진단 도구
