version: "2"

cloud:
  organization: ""
  project: "mombank"
  hostname: ""

sql:
  - engine: "postgresql"
    queries: "./db/query/"
    schema: "./db/migration/"
    gen:
      go:
        package: "db"
        out: "./db/sqlc"
#        sql_package: "pgx/v5"
        emit_json_tags: true
overrides:
  go: null
plugins: [ ]
rules: [ ]
options: { }

